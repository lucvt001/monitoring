/**/mqtt_client:
  ros__parameters:
    broker:
      host: '6568851381634f73af5c5631e79edcce.s1.eu.hivemq.cloud'
      port: 8883
      user: 'ground_station1'
      pass: 'Ground_station1'
      tls:
        enabled: true

    bridge:
      mqtt2ros:
        mqtt_topics: 
          - 'mqtt_client/origin_gps'
          - 'mqtt_client/MiniUSV1/sensor/gps'
          - 'mqtt_client/MiniUSV1/sensor/heading'
          - 'mqtt_client/MiniUSV2/sensor/gps'
          - 'mqtt_client/MiniUSV2/sensor/heading'
          - 'mqtt_client/MiniUSV2/control/throttle'
          - 'mqtt_client/MiniUSV2/control/steering'
        mqtt_client/origin_gps:
          ros_topic: 'origin_gps'
        mqtt_client/MiniUSV1/sensor/gps:
          ros_topic: 'usv1/gps'
        mqtt_client/MiniUSV1/sensor/heading:
          ros_topic: 'usv1/heading'
        mqtt_client/MiniUSV2/sensor/gps:
          ros_topic: 'usv2/gps'
        mqtt_client/MiniUSV2/sensor/heading:
          ros_topic: 'usv2/heading'
        mqtt_client/MiniUSV2/control/throttle:
          ros_topic: 'follower/control/throttle'
        mqtt_client/MiniUSV2/control/steering:
          ros_topic: 'follower/control/steering'